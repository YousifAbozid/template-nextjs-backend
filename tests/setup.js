// Set up environment variables for testing
// process.env.MONGODB_URI = 'mongodb://localhost:27017/test-db';
// process.env.JWT_SECRET = 'test-jwt-secret';
// process.env.JWT_EXPIRES_IN = '1h';
// process.env.API_VERSION = '1.0.0';
// process.env.RATE_LIMIT_WINDOW_MS = '60000';
// process.env.RATE_LIMIT_MAX_REQUESTS = '60';
// process.env.RATE_LIMIT_STRICT_MAX = '10';
// process.env.RATE_LIMIT_API_MAX = '120';

// Global beforeAll and afterAll hooks can be added here
beforeAll(async () => {
  // Setup code that runs before all tests
  // For example: connect to a test database, seed test data, etc.
});

afterAll(async () => {
  // Cleanup code that runs after all tests
  // For example: disconnect from test database, clean up test data, etc.
});
